pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build Stage'
                }
        }
         stage('Deploy') {
            steps {
                echo 'Deploy'
                }
         }
         stage('Test') {
            steps {
                echo 'Test'
            }
        }
    }
}
